/*
(function ($) {
  module('jQuery.alertJs');

  test('is alertJs', function () {
    expect(2);
    strictEqual($.alertJs(), 'alertJs.', 'should be alertJs');
    strictEqual($.alertJs({punctuation: '!'}), 'alertJs!', 'should be thoroughly alertJs');
  });
}(jQuery));
*/
jQuery(function($){
  $.alert('这是个测试这是个测试这是个测试这是个测试这是个测试这是个测试这是个测试这是个测试这是个测试');
});